1. **Pre-process sound loops with scene effects**: Modify `_activate_scene` in `voice_modulator.py` to process the sound loop data (`sf.read` output) with the scene's configured effects before storing it in `_active_loops`. This ensures the effects apply *only* to the specific sound loop and not globally. We will create a local `pedalboard.Pedalboard` inside `_activate_scene` using the scene effects, process the `data` array with it, and store the resulting array in `self._active_loops`.
2. **Remove `self._active_effects`**: Since scene effects are now baked into the loop audio data, remove the `self._active_effects` dictionary. Delete its initialization, and usages in `_activate_scene`, `_deactivate_scene`, `deactivate_all_scenes`, and `_rebuild_pedalboard`. This prevents the scene effects from being added to the global `_audio_board` and applying to the user's voice stream.
3. **Verify the code edits**: Run `read_file` or `grep` to verify the modified sections of `voice_modulator.py` to confirm the edits were written correctly.
4. **Test the application**: Run the headless application test command (`QT_QPA_PLATFORM=offscreen python3 voice_modulator.py & sleep 10 && pkill -f voice_modulator.py`) to verify the audio processing works without crashing.
5. **Complete pre commit steps**: Complete pre commit steps to ensure proper testing, verification, review, and reflection are done.
6. **Submit the change**: Submit the code changes.
